# ── Vibedeck backend dependencies ─────────────────────────────
# Pin major/minor; let patch float. Python 3.11+.

fastapi==0.115.*
uvicorn[standard]==0.34.*

# ORM + migrations (SQLAlchemy 2.0 style)
sqlalchemy==2.0.*
alembic==1.14.*
psycopg[binary]==3.2.*          # PostgreSQL driver

# Settings & validation (Pydantic v2)
pydantic==2.*
pydantic-settings==2.*

# Markdown deck parsing
# The parser does its own block splitting (deck fm + [card-fm, card-body]
# pairs), so we use PyYAML directly rather than the single-document
# python-frontmatter library.
pyyaml==6.0.*                   # YAML frontmatter parsing
python-multipart==0.0.*         # file upload form parsing

# Auth (JWT + password hashing)
pyjwt==2.10.*
# bcrypt directly rather than via passlib: passlib 1.7.x is unmaintained
# and fails outright with bcrypt 5.x. bcrypt's own API is simple enough.
bcrypt==5.0.*
