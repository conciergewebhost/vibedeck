---
/** ConceptCard — a single idea, explained cleanly. */
interface Props { html: string; }
const { html } = Astro.props;
---
<div class="card-body card-body--concept" data-card-type="concept" set:html={html} />
