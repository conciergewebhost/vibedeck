---
/**
 * 404 page — served for unmatched routes (SSR) and reused by the
 * topic/deck pages via Astro.rewrite("/404").
 */
import NotFound from "../components/NotFound.astro";

Astro.response.status = 404;
---

<NotFound />
